 import UIKit
 
 var str = "Hello, Swift!!"
 //프로젝트 이름은 가급적 영 대소문자, 숫자만을 이용한다
 //공백, 특수문자 사용의 경우 경로문제의 문제
 //저장위치가 오토세이브 -> 파일 찾기 어려움
 
 //cmd + '+' -> 폰트 키우기, '-'는 줄이기
 
 print(str)  //실행 후 나타나는 아래 영역은 디버그 영역(콘솔)이라 한다
 
 print(str)
 // 우측 회색 영역을 result sidebar 이라고 한다
 //result bar에 있는 우측 아이콘을 클릭하면 result view 가 실행된다
 
 var sum = 0
 for i in 1...10{   //
    sum += i
 }
 print(sum)
 
 let r = CGRect(x: 0, y: 10, width: 20, height: 30)
 //CGRect = 프레임의 높이 너비 등의 값을 저장
 
 
 
